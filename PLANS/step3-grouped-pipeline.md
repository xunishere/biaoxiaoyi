# Step 3 待实现：分组流水线（方案 2+3 结合）

> 记录于 2026-05-09 14:59  
> 状态：待实现（用户先跑一遍当前逻辑）

## 流程

```
① 按评分大类给目录叶子节点分组
     ↓
   ┌─ 大类1 ──────────────────────────────┐
   │ 生成该组所有叶子 → 检 → 评 → 优       │
   └──────────────────────────────────────┘
     ↓ 前组内容摘要喂给下组（避免重复）
   ┌─ 大类2 ──────────────────────────────┐
   │ 生成 → 检 → 评 → 优                   │
   └──────────────────────────────────────┘
     ↓
   ... 逐大类推进 ...
     ↓
② 全部完成后：全文总评分 + 总缺口检查
     ↓
③ 导出
```

## 改动清单

| 层 | 改动 | 说明 |
|----|------|------|
| 后端 | 基本不改 | 现有 review 端点可复用，前端调时机不同 |
| 前端 ContentEdit.tsx | 重构生成逻辑 | 按组推进；组间传上下文摘要；操作栏串行推进 |

## 关键点

- 分组依据：Step 2 aligned 模式的 `requirement_groups`（评分大类）
- 组间防重复：生成下一组时 prompt 带前几组已生成内容的摘要
- 单组内串行：生成→缺口分析→评分→优化 一个按钮推进
- 全部完成后：一次全量总评分 + 总缺口检查
